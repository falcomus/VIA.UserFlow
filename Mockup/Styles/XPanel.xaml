<!--  XPanel  -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Mockup.UIControls">

    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#FFF7F9FC" />

    <SolidColorBrush x:Key="PanelSurfaceBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="PanelSurfaceBorderBrush" Color="#FFD8E0EA" />

    <Style TargetType="{x:Type local:XPanel}">
        <Setter Property="Background" Value="{StaticResource PanelSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorder}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:XPanel}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
