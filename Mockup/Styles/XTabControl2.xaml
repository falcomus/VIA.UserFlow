<!--  XTabControl secondary  -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ext="clr-namespace:Mockup.Extensions"
    xmlns:via="http://schemas.via.dev/wpf">

    <Style x:Key="CustomTabItemStyle2" TargetType="TabItem">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static via:XBrushKeys.NavigationPanelForeground}}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Height" Value="38" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Padding" Value="14,0,14,1" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid
                        VerticalAlignment="Center"
                        ext:GridEx.RowDef="*,Auto"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <!--<Border
                            x:Name="HoverSurface"
                            Margin="4,6,4,5"
                            Background="Transparent"
                            CornerRadius="6" />-->

                        <ContentPresenter
                            x:Name="ContentSite"
                            Margin="{TemplateBinding Padding}"
                            VerticalAlignment="Center"
                            ContentSource="Header"
                            Cursor="Hand"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="True"
                            TextElement.FontFamily="{TemplateBinding FontFamily}"
                            TextElement.FontSize="{TemplateBinding FontSize}"
                            TextElement.FontWeight="{TemplateBinding FontWeight}"
                            TextElement.Foreground="{TemplateBinding Foreground}" />

                        <Border
                            x:Name="Underline"
                            Grid.Row="1"
                            Height="1.5"
                            Margin="14,0,14,0"
                            VerticalAlignment="Top"
                            Background="Transparent"
                            CornerRadius="1" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{DynamicResource {x:Static via:XBrushKeys.NavigationPanelSelectedItemForeground}}" />
                            <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold" />
                            <Setter TargetName="Underline" Property="Background" Value="{DynamicResource {x:Static via:XBrushKeys.Primary}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{DynamicResource {x:Static via:XBrushKeys.NavigationPanelSelectedItemForeground}}" />
                            <!--<Setter TargetName="HoverSurface" Property="Background" Value="#12FFFFFF" />-->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentSite" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CustomTabControlStyle2" TargetType="TabControl">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid
                        KeyboardNavigation.TabNavigation="Local"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border
                            Grid.Row="0"
                            Background="{DynamicResource {x:Static via:XBrushKeys.NavigationPanelBackground}}"
                            BorderBrush="{DynamicResource {x:Static via:XBrushKeys.NavigationPanelBorder}}"
                            BorderThickness="0,0,0,1"
                            SnapsToDevicePixels="True">
                            <TabPanel
                                x:Name="HeaderPanel"
                                Margin="6,0,0,0"
                                HorizontalAlignment="Left"
                                Panel.ZIndex="1"
                                IsItemsHost="True"
                                KeyboardNavigation.TabIndex="1" />
                        </Border>

                        <ContentPresenter
                            x:Name="PART_SelectedContentHost"
                            Grid.Row="1"
                            ContentSource="SelectedContent"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
