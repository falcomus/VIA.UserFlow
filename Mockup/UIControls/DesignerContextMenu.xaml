<!--
    ======================================================================================
    FILE: Mockup.UIControls/DesignerContextMenu.xaml
    ======================================================================================
-->

<via:XContextMenu
  x:Class="Mockup.UIControls.DesignerContextMenu"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:ico="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:resources="clr-namespace:Mockup.Resources"
  xmlns:via="http://schemas.via.dev/wpf"
  xmlns:viewmodel="clr-namespace:Mockup.ViewModel"
  d:DataContext="{d:DesignInstance Type=viewmodel:MockupViewModel}"
  mc:Ignorable="d"
>
  <!--  =========================================================  -->
  <!--  SCREEN ROOT (nur wenn KEIN Control-/AA-Kontext)  -->
  <!--  =========================================================  -->

  <via:XMenuItem
    Command="{Binding EditScreenCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="{via:XLoc ContextMenu.Screen.Edit, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=PencilOutline}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding NewScreenCommand}"
    Header="{via:XLoc ContextMenu.Screen.New, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=Plus}"
    Variant="Primary"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding PasteControlsCommand}"
    Header="{via:XLoc ContextMenu.Control.Paste, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentPaste}"
    IsEnabled="{Binding CanPasteControls}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding DeleteScreenCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="{via:XLoc ContextMenu.Screen.Delete, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=DeleteOutline}"
    Variant="Danger"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding ChangeScreenBackgroundCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="{via:XLoc ContextMenu.Screen.ChangeBackground, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=PaletteOutline}"
    IsEnabled="True"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding SelectScreenImageCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="{via:XLoc ContextMenu.Screen.SelectImage, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=FolderOpenOutline}"
    IsEnabled="False"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Screen.ShowHeader, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ViewHeadline}"
    IsCheckable="True"
    IsChecked="{Binding CurrentScreen.ShowHeader, Mode=TwoWay}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Screen.ShowFooter, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ViewHeadline}"
    IsCheckable="True"
    IsChecked="{Binding CurrentScreen.ShowFooter, Mode=TwoWay}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <!--  =========================================================  -->
  <!--  TEMPLATE ROOT (nur wenn KEIN Control-/AA-Kontext)  -->
  <!--  =========================================================  -->

  <via:XMenuItem
    Command="{Binding EditTemplateCommand}"
    CommandParameter="{Binding CurrentTemplate}"
    Header="{via:XLoc ContextMenu.Template.Edit, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=PencilOutline}"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding NewTemplateCommand}"
    Header="{via:XLoc ContextMenu.Template.New, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=Plus}"
    Variant="Primary"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding PasteControlsCommand}"
    Header="{via:XLoc ContextMenu.Control.Paste, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentPaste}"
    IsEnabled="{Binding CanPasteControls}"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding DeleteTemplateCommand}"
    CommandParameter="{Binding CurrentTemplate}"
    Header="{via:XLoc ContextMenu.Template.Delete, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=DeleteOutline}"
    Variant="Danger"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <!--  =========================================================  -->
  <!--  POPUP ROOT (nur wenn KEIN Control-/AA-Kontext)  -->
  <!--  =========================================================  -->

  <via:XMenuItem
    Command="{Binding EditPopupCommand}"
    CommandParameter="{Binding CurrentPopup}"
    Header="{via:XLoc ContextMenu.Popup.Edit, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=PencilOutline}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding NewPopupCommand}"
    Header="{via:XLoc ContextMenu.Popup.New, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=Plus}"
    Variant="Primary"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding PasteControlsCommand}"
    Header="{via:XLoc ContextMenu.Control.Paste, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentPaste}"
    IsEnabled="{Binding CanPasteControls}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding DeletePopupCommand}"
    CommandParameter="{Binding CurrentPopup}"
    Header="{via:XLoc ContextMenu.Popup.Delete, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=DeleteOutline}"
    Variant="Danger"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Popup.DockLeft, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ChevronLeft}"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Left}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Popup.DockRight, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ChevronRight}"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Right}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Popup.DockTop, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ChevronUp}"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Top}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Popup.DockBottom, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ChevronDown}"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Bottom}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Popup.DockCenter, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=VectorSquare}"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Center}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Header="{via:XLoc ContextMenu.Popup.AtMousePosition, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ApplicationOutline}"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=MousePos}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <!--  =========================================================  -->
  <!--  BAND MENU (nur wenn Band-Kontext & KEIN Control-/AA-Kontext)  -->
  <!--  =========================================================  -->

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowSeparator_RootToBand, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding MoveBandUpCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="{via:XLoc ContextMenu.Band.MoveUp, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ArrowUp}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding MoveBandDownCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="{via:XLoc ContextMenu.Band.MoveDown, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ArrowDown}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding AddBandCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="{via:XLoc ContextMenu.Band.New, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=Plus}"
    Variant="Primary"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <!--<via:XMenuItem
    Command="{Binding PasteControlsCommand}"
    Header="Paste Control(s)"
    IsEnabled="{Binding CanPasteControls}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <via:XMenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentPaste" />
    </via:XMenuItem.Icon>
  </via:XMenuItem>-->

  <via:XMenuItem
    Command="{Binding DuplicateBandCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="{via:XLoc ContextMenu.Band.Duplicate, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentCopy}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding DeleteBandCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="{via:XLoc ContextMenu.Band.Delete, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=DeleteOutline}"
    Variant="Danger"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <!--  =========================================================  -->
  <!--  ACTION AREA MENU (exklusiv für einzelne ActionArea)  -->
  <!--  =========================================================  -->

  <via:XMenuItem
    Command="{Binding EditActionAreaActionsCommand}"
    CommandParameter="{Binding CurrentActionArea}"
    Header="{via:XLoc ContextMenu.ActionArea.EditActions, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=PencilOutline}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding CopyControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.ActionArea.Copy, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentCopy}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding PasteControlsCommand}"
    Header="{via:XLoc ContextMenu.ActionArea.Paste, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentPaste}"
    IsEnabled="{Binding CanPasteControls}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding DeleteControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.ActionArea.Delete, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=DeleteOutline}"
    Variant="Danger"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <!--  =========================================================  -->
  <!--  CONTROL MENU (exklusiv)  -->
  <!--  =========================================================  -->

  <via:XMenuItem
    Command="{Binding CopyControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.Control.Copy, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentCopy}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding PasteControlsCommand}"
    Header="{via:XLoc ContextMenu.Control.Paste, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ContentPaste}"
    IsEnabled="{Binding CanPasteControls}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding DeleteControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.Control.Delete, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=DeleteOutline}"
    Variant="Danger"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator Style="{DynamicResource XMenuSeparatorStyle}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding BringToFrontCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.Control.BringToFront, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ArrowUp}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding SendToBackCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.Control.SendToBack, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ArrowDown}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding BringForwardCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.Control.BringForward, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ChevronUp}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <via:XMenuItem
    Command="{Binding SendBackwardCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="{via:XLoc ContextMenu.Control.SendBackward, ResourceManager={x:Static resources:UserFlowResources.ResourceManager}}"
    Icon="{via:MaterialIcon Kind=ChevronDown}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />
</via:XContextMenu>

<!--
    ======================================================================================
    FILE: Mockup.UIControls/DesignerContextMenu.xaml
    ======================================================================================
-->
<!--

<ContextMenu
  x:Class="Mockup.UIControls.DesignerContextMenu"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:ico="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewmodel="clr-namespace:Mockup.ViewModel"
  d:DataContext="{d:DesignInstance Type=viewmodel:MockupViewModel}"
  Style="{StaticResource XContextMenu}"
  mc:Ignorable="d"
>
-->
<!--  FontWeight="Medium"  -->
<!--  Padding="0"  -->
<!--  FontSize="14"  -->

<!--  =========================================================  -->
<!--  SCREEN ROOT (nur wenn KEIN Control-Kontext)  -->
<!--  =========================================================  -->
<!--

  <MenuItem
    Command="{Binding EditScreenCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="Edit Screen"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Edit" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding NewScreenCommand}"
    Header="New Screen"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Add" />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--<MenuItem
    Command="{Binding DuplicateScreenCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="Duplicate Screen"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>-->
<!--

  <MenuItem
    Command="{Binding DeleteScreenCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="Delete Screen"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign
        Width="12"
        Height="12"
        Foreground="{DynamicResource DangerBrush}"
        Kind="Delete"
      />
    </MenuItem.Icon>
  </MenuItem>

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding ChangeScreenBackgroundCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="Change Screen Background"
    IsEnabled="True"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="ColorLens" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding SelectScreenImageCommand}"
    CommandParameter="{Binding CurrentScreen}"
    Header="Select Screen Image"
    IsEnabled="False"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Show Header"
    IsCheckable="True"
    IsChecked="{Binding CurrentScreen.ShowHeader, Mode=TwoWay}"
    Style="{StaticResource XMenuItemCheck}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Show Footer"
    IsCheckable="True"
    IsChecked="{Binding CurrentScreen.ShowFooter, Mode=TwoWay}"
    Style="{StaticResource XMenuItemCheck}"
    Visibility="{Binding ShowScreenMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

-->
<!--  =========================================================  -->
<!--  TEMPLATE ROOT (nur wenn KEIN Control-Kontext)  -->
<!--  =========================================================  -->
<!--

  <MenuItem
    Command="{Binding EditTemplateCommand}"
    CommandParameter="{Binding CurrentTemplate}"
    Header="Edit Template"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Edit" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding NewTemplateCommand}"
    Header="New Template"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Add" />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--<MenuItem
    Command="{Binding DuplicateTemplateCommand}"
    CommandParameter="{Binding CurrentTemplate}"
    Header="Duplicate Template"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>-->
<!--

  <MenuItem
    Command="{Binding DeleteTemplateCommand}"
    CommandParameter="{Binding CurrentTemplate}"
    Header="Delete Template"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowTemplateMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign
        Width="12"
        Height="12"
        Foreground="{DynamicResource DangerBrush}"
        Kind="Delete"
      />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--  =========================================================  -->
<!--  POPUP ROOT (nur wenn KEIN Control-Kontext)  -->
<!--  =========================================================  -->
<!--

  <MenuItem
    Command="{Binding EditPopupCommand}"
    CommandParameter="{Binding CurrentPopup}"
    Header="Edit Popup"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Edit" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding NewPopupCommand}"
    Header="New Popup"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Add" />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--<MenuItem
    Command="{Binding DuplicatePopupCommand}"
    CommandParameter="{Binding CurrentPopup}"
    Header="Duplicate Popup"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>-->
<!--

  <MenuItem
    Command="{Binding DeletePopupCommand}"
    CommandParameter="{Binding CurrentPopup}"
    Header="Delete Popup"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign
        Width="12"
        Height="12"
        Foreground="{DynamicResource DangerBrush}"
        Kind="Delete"
      />
    </MenuItem.Icon>
  </MenuItem>

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Dock Left"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Left}"
    Style="{StaticResource XMenuItemRadio}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Dock Right"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Right}"
    Style="{StaticResource XMenuItemRadio}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Dock Top"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Top}"
    Style="{StaticResource XMenuItemRadio}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Dock Bottom"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Bottom}"
    Style="{StaticResource XMenuItemRadio}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="Dock Center"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Center}"
    Style="{StaticResource XMenuItemRadio}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Header="At Mouse Position"
    IsCheckable="True"
    IsChecked="{Binding CurrentPopup.Position, Mode=TwoWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=MousePos}"
    Style="{StaticResource XMenuItemRadio}"
    Visibility="{Binding ShowPopupMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

-->
<!--  =========================================================  -->
<!--  BAND MENU (nur wenn Band-Kontext & KEIN Control-Kontext)  -->
<!--  =========================================================  -->
<!--

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowSeparator_RootToBand, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding MoveBandUpCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="Move Band Up"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="ArrowUpward" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding MoveBandDownCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="Move Band Down"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="ArrowDownward" />
    </MenuItem.Icon>
  </MenuItem>

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding AddBandCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="New Band"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Add" />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--<MenuItem
        Command="{Binding AddExpandableBandCommand}"
        CommandParameter="{Binding ContextBand}"
        Header="New expandable Band"
        Style="{StaticResource XMenuItem}"
        Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}">
        <MenuItem.Icon>
            <ico:PackIconMaterialDesign
                Width="12"
                Height="12"
                Kind="Add" />
        </MenuItem.Icon>
    </MenuItem>-->
<!--

  <MenuItem
    Command="{Binding DuplicateBandCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="Duplicate Band"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding DeleteBandCommand}"
    CommandParameter="{Binding ContextBand}"
    Header="Delete Band"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowBandMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign
        Width="12"
        Height="12"
        Foreground="{DynamicResource DangerBrush}"
        Kind="Delete"
      />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--  =========================================================  -->
<!--  ACTION AREA MENU (exklusiv für einzelne ActionArea)  -->
<!--  =========================================================  -->
<!--

  <MenuItem
    Command="{Binding EditActionAreaActionsCommand}"
    CommandParameter="{Binding CurrentActionArea}"
    Header="Edit Actions"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="12" Height="12" Kind="Edit" />
    </MenuItem.Icon>
  </MenuItem>

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

-->
<!--<MenuItem
    Command="{Binding DuplicateControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Copy ActionArea"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>-->
<!--

  <MenuItem
    Command="{Binding CopyControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Copy ActionArea"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding PasteControlsCommand}"
    Header="Paste ActionArea"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
    IsEnabled="{Binding CanPasteControls}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentPaste" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding DeleteControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Delete ActionArea"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowActionAreaMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign
        Width="12"
        Height="12"
        Foreground="{DynamicResource DangerBrush}"
        Kind="Delete"
      />
    </MenuItem.Icon>
  </MenuItem>

-->
<!--  =========================================================  -->
<!--  CONTROL MENU (exklusiv)  -->
<!--  =========================================================  -->

<!--<MenuItem
        Command="{Binding EditControlCommand}"
        CommandParameter="{Binding ContextControls}"
        Header="Edit Control"
        IsEnabled="False"
        Style="{StaticResource XMenuItem}"
        Visibility="{Binding HasSingleControlContext, Converter={StaticResource BoolToVisibilityConverter}}">
        <MenuItem.Icon>
            <ico:PackIconMaterialDesign
                Width="12"
                Height="12"
                Kind="Edit" />
        </MenuItem.Icon>
    </MenuItem>-->

<!--<MenuItem
    Command="{Binding DuplicateControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Duplicate Control(s)"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>-->
<!--

  <MenuItem
    Command="{Binding CopyControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Copy Control(s)"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentCopy" />
    </MenuItem.Icon>
  </MenuItem>

  <MenuItem
    Command="{Binding PasteControlsCommand}"
    Header="Paste Control(s)"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
    IsEnabled="{Binding CanPasteControls}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign Width="14" Height="14" Kind="ContentPaste" />
    </MenuItem.Icon>
  </MenuItem>

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding DeleteControlsCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Delete Control(s)"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  >
    <MenuItem.Icon>
      <ico:PackIconMaterialDesign
        Width="12"
        Height="12"
        Foreground="{DynamicResource DangerBrush}"
        Kind="Delete"
      />
    </MenuItem.Icon>
  </MenuItem>

  <Separator
    Style="{StaticResource XMenuSeparator}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding BringToFrontCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Bring To Front"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding SendToBackCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Send To Back"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding BringForwardCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Bring Forward"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />

  <MenuItem
    Command="{Binding SendBackwardCommand}"
    CommandParameter="{Binding ContextControls}"
    Header="Send Backward"
    Style="{StaticResource XMenuItem}"
    Visibility="{Binding ShowControlMenu, Converter={StaticResource BoolToVisibilityConverter}}"
  />
</ContextMenu>-->