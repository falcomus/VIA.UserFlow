<UserControl
    x:Class="Mockup.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:Mockup.Extensions"
    xmlns:ico="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Mockup.UIControls"
    xmlns:via="http://schemas.via.dev/wpf"
    d:DesignHeight="850"
    d:DesignWidth="800"
    Background="{DynamicResource DesignerBackgroundBrush}"
    Foreground="{DynamicResource TextPrimaryBrush}"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType"
    mc:Ignorable="d">

    <Grid ext:GridEx.RowDef="45,*">
        <Border
            Background="{DynamicResource HeaderBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="8"
                    Direction="270"
                    Opacity="0.12"
                    ShadowDepth="2" />
            </Border.Effect>
            <Grid Margin="16,0" ext:GridEx.ColDef="Auto,*">
                <ico:PackIconFontAwesome5
                    Width="20"
                    Height="20"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource PrimaryBrush}"
                    Kind="QuestionCircleRegular" />
                <TextBlock
                    Grid.Column="1"
                    Margin="10,0,0,1"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Text="About VIA UserFlow" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Border
                Width="590"
                MaxWidth="780"
                Padding="26"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{DynamicResource {x:Static via:XBrushKeys.SurfaceSunken}}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="18"
                        Direction="315"
                        Opacity="0.08"
                        ShadowDepth="3" />
                </Border.Effect>

                <Grid ext:GridEx.RowDef="Auto,Auto,*">
                    <Border
                        Width="48"
                        Height="48"
                        HorizontalAlignment="Left"
                        Background="{DynamicResource PrimaryBrush}"
                        CornerRadius="12">
                        <ico:PackIconFontAwesome5
                            Width="25"
                            Height="25"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource {x:Static via:XBrushKeys.PrimaryForeground}}"
                            Kind="InfoCircleSolid" />
                    </Border>

                    <StackPanel Grid.Row="1" Margin="0,18,0,18">
                        <TextBlock
                            FontSize="24"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            Text="About VIA UserFlow" />
                        <TextBlock
                            Margin="0,8,0,0"
                            FontSize="14"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="Mockup and prototype design tool for WPF-based product planning."
                            TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <Border
                            Margin="0,0,0,10"
                            Padding="16,14,16,14"
                            Background="{DynamicResource {x:Static via:XBrushKeys.SurfaceRaised}}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Grid ext:GridEx.ColDef="Auto,*">
                                <Border
                                    Width="32"
                                    Height="32"
                                    Background="{DynamicResource {x:Static via:XBrushKeys.PrimarySubtle}}"
                                    CornerRadius="16">
                                    <ico:PackIconFontAwesome5
                                        Width="16"
                                        Height="16"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource PrimaryBrush}"
                                        Kind="CheckCircleRegular" />
                                </Border>
                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Text="Purpose" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="13"
                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                        Text="Design screens, templates and popups in a single focused desktop tool."
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border
                            Margin="0,0,0,10"
                            Padding="16,14,16,14"
                            Background="{DynamicResource {x:Static via:XBrushKeys.SurfaceRaised}}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Grid ext:GridEx.ColDef="Auto,*">
                                <Border
                                    Width="32"
                                    Height="32"
                                    Background="{DynamicResource {x:Static via:XBrushKeys.PrimarySubtle}}"
                                    CornerRadius="16">
                                    <ico:PackIconFontAwesome5
                                        Width="16"
                                        Height="16"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource PrimaryBrush}"
                                        Kind="CheckCircleRegular" />
                                </Border>
                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Text="Design direction" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="13"
                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                        Text="The UI is being refreshed without changing the underlying project architecture."
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border
                            Margin="0,0,0,10"
                            Padding="16,14,16,14"
                            Background="{DynamicResource {x:Static via:XBrushKeys.SurfaceRaised}}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Grid ext:GridEx.ColDef="Auto,*">
                                <Border
                                    Width="32"
                                    Height="32"
                                    Background="{DynamicResource {x:Static via:XBrushKeys.PrimarySubtle}}"
                                    CornerRadius="16">
                                    <ico:PackIconFontAwesome5
                                        Width="16"
                                        Height="16"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource PrimaryBrush}"
                                        Kind="CheckCircleRegular" />
                                </Border>
                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Text="Technology" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="13"
                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                        Text="Built with VIA.WPF, WPF, SkiaSharp and the existing VIA UserFlow model."
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
